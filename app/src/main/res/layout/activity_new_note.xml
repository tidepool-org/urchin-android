<android.support.v4.widget.DrawerLayout
    android:id="@+id/drawer_layout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The main content view -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="io.tidepool.urchin.NewNoteActivity">

        <!-- Date / time view -->
        <LinearLayout
            android:id="@+id/date_time_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/activity_vertical_margin"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:paddingRight="@dimen/activity_horizontal_margin"
            android:layout_alignParentTop="true"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/date_time"
                style="@android:style/TextAppearance.Medium"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="0.75"
                android:text="Monday 8/31/15 10:48 AM"/>

            <TextView
                android:id="@+id/change_label"
                style="@android:style/TextAppearance.Small"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical|end"
                android:layout_weight="0.25"
                android:gravity="end"
                android:text="@string/edit"
                android:textColor="@color/link"/>
        </LinearLayout>

        <!-- Separator -->
        <View
            android:id="@+id/sep1"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:layout_below="@id/date_time_layout"
            android:layout_marginBottom="4dp"
            android:layout_marginTop="4dp"
            android:background="@color/hashtag_separator"/>

        <!-- Edit area -->

        <MultiAutoCompleteTextView
            android:id="@+id/note_edit_text"
            style="@android:style/TextAppearance.Large"

            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/sep1"
            android:layout_above="@+id/hashtag_button"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:background="@color/new_note_bg"
            android:gravity="start|top"
            android:hint="@string/add_your_note_here"
            android:inputType="textMultiLine|textCapSentences"
            android:minHeight="120sp"
            android:padding="8dp"
            >

        </MultiAutoCompleteTextView>

        <Button
            android:id="@+id/hashtag_button"
            android:gravity="center_horizontal|center_vertical"
            android:layout_width="@dimen/hashtag_button_diameter"
            android:layout_height="@dimen/hashtag_button_diameter"
            android:layout_alignParentStart="true"
            android:layout_alignParentBottom="true"
            android:layout_marginStart="@dimen/hashtag_button_start_offset"
            android:layout_marginBottom="@dimen/hashtag_button_start_offset"
            android:background="@drawable/hashtag_button"
            android:elevation="@dimen/add_button_elevation"
            android:text="#"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="@color/hashtag_button_fg"
            />

    </RelativeLayout>


    <!-- Hidden drop-down menu for selecting a user -->
    <LinearLayout
        android:id="@+id/layout_drop_down"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true"
        android:background="@color/drop_down_bg"
        android:orientation="vertical"
        android:visibility="invisible">

        <ListView
            android:id="@+id/listview_filter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/dropdown_list_bg"/>

    </LinearLayout>

    <!-- The navigation drawer -->
    <ListView
        android:id="@+id/left_drawer"
        android:layout_width="240dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#111"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp"/>

</android.support.v4.widget.DrawerLayout>